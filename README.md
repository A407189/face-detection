# Face Detection Project

This project uses Python and OpenCV to detect faces using a webcam.

## Features
- Real-time face detection
- Uses Haar Cascade

## How to run
1. Install OpenCV:
pip install opencv-python

2. Run:
python face.py
